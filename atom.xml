<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[logan computes.]]></title>
  <link href="http://loganhasson.github.io/atom.xml" rel="self"/>
  <link href="http://loganhasson.github.io/"/>
  <updated>2013-10-11T10:27:03-04:00</updated>
  <id>http://loganhasson.github.io/</id>
  <author>
    <name><![CDATA[Logan Hasson]]></name>
    <email><![CDATA[loganhasson@gmail.com]]></email>
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Dot(.) Send Me to the Moon]]></title>
    <link href="http://loganhasson.github.io/blog/2013/10/10/dot-send-me-to-the-moon/"/>
    <updated>2013-10-10T18:20:00-04:00</updated>
    <id>http://loganhasson.github.io/blog/2013/10/10/dot-send-me-to-the-moon</id>
    <content type="html"><![CDATA[<p>One of the greatest things about Ruby is its never-ending ability to momentarily bewilder you before exploding your brain all over your face. Whether it&rsquo;s a seemingly supurfluous bit of syntactic sugar (point for alliteration to me, in case you&rsquo;re counting) or a questionable method call where one doesn&rsquo;t seem necessary, Ruby constantly has me scratching my head mere seconds before blowing me away.</p>

<p>My most recent moment of <em>ermahgerd</em> came when discovering the <code>.send</code> method. Here&rsquo;s what it looks like when called on an <code>Array</code>:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
</pre></td><td class='code'><pre><code class='ruby'><span class='line'><span class="n">array</span> <span class="o">=</span> <span class="o">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="o">]</span>
</span><span class='line'><span class="n">array</span><span class="o">.</span><span class="n">send</span><span class="p">(</span><span class="ss">:[]=</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">5</span><span class="p">)</span>
</span><span class='line'>
</span><span class='line'><span class="o">=&gt;</span> <span class="o">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">5</span><span class="o">]</span>
</span></code></pre></td></tr></table></div></figure>


<p>(The parenthesis aren&rsquo;t necessary, but I like them for clarity. Up in the code. Not surrounding this sentence, silly.)</p>

<p>So what exactly is this doing? It looks wacky, fo sho. To break it down, let&rsquo;s look at its equilavent in the way we normal human beings would most likely write it:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
</pre></td><td class='code'><pre><code class='ruby'><span class='line'><span class="n">array</span> <span class="o">=</span> <span class="o">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="o">]</span>
</span><span class='line'><span class="n">array</span><span class="o">[</span><span class="mi">4</span><span class="o">]</span> <span class="o">=</span> <span class="mi">5</span>
</span><span class='line'>
</span><span class='line'><span class="o">=&gt;</span> <span class="o">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">5</span><span class="o">]</span>
</span></code></pre></td></tr></table></div></figure>


<p>So yeah, that&rsquo;s different. To understand what&rsquo;s going on with <code>.send</code>, which, by the way, &lsquo;sends a message to a receiver&rsquo;, we have to remember that <code>array[whatever]</code> is really <code>array.[]=(index, value)</code>. Just because brackets are involved doesn&rsquo;t mean a method isn&rsquo;t being called (or that a message isn&rsquo;t being sent). <code>.send</code> then, basically passes a method to an object, along with a couple of arguments, and then invokes that method.</p>

<p><strong>Mini-break</strong>: What does this whole &ldquo;sends a message to a receiver&rdquo; or &ldquo;passes a method to an object&rdquo; stuff mean? At first, it&rsquo;s kind of annoy&hellip;erm&hellip;I mean difficult to understand the oft-repeated refrain that &ldquo;everything in Ruby is an object.&rdquo; First of all, what the heck is an object? And second of all, so what? Here&rsquo;s the quick and dirty deets, supremely over-simplified—when we say &ldquo;object&rdquo; we really just mean &ldquo;thing&rdquo;. And now I&rsquo;m guilty of saying it myself: everything really is an object. Like, <em>all the things</em> are ojects. Every last one of them. And they&rsquo;re just things that chill and wait for you to tell them to do stuff. Which is really cool, because all of these things can respond to a zillion different methods. And methods are little messages you &ldquo;send&rdquo; to these objects that tell them to do stuff. An object either understands a method or it doesn&rsquo;t. So if we think about it really abstractly, let&rsquo;s say I&rsquo;m a Ruby object. (Sweet!) You could send me a method like <code>.karate</code> and I&rsquo;d look at you like you&rsquo;re crazy because my parents never let me do martial arts when I was younger and that was sad and I&rsquo;ll stop rambling about that right now. But if you sent me a method like <code>.drink_water</code> I&rsquo;d know exactly what to do—I can drink a glass of water like a boss—and I&rsquo;d do it. This, in a nutshell, is what objects and methods are all about in Ruby. (Well, kinda. Maybe. You know what? Just keep moving&hellip;)</p>

<p><strong>Now, back to your regular programming (hah!)</strong></p>

<p>Cool.</p>

<p>Wait, why is it cool? Doesn&rsquo;t it seem like an unnecessary step? After all, as we just decided, the following code would look just as weird, get the same thing done, and not use the <code>.send</code> method at all:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
</pre></td><td class='code'><pre><code class='ruby'><span class='line'><span class="n">array</span> <span class="o">=</span> <span class="o">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="o">]</span>
</span><span class='line'><span class="n">array</span><span class="o">.</span><span class="n">[</span><span class="o">]=</span><span class="p">(</span><span class="mi">4</span><span class="p">,</span> <span class="mi">5</span><span class="p">)</span>
</span><span class='line'>
</span><span class='line'><span class="o">=&gt;</span> <span class="o">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">5</span><span class="o">]</span>
</span></code></pre></td></tr></table></div></figure>


<p>(Ignore the weird syntax highlighting on that first bracket on line two—I think I broke the internet or something.)</p>

<p>So why bring <code>.send</code> into the mix at all? It just seems like extra…stuff, right? Well, what happens if we think about dynamically calling methods based on stuff that happens in our code? Or, even better, what if we want to call methods depending on user input? Then we can do some <em>really</em> cool stuff.</p>

<p>Well, I mean, somebody could do really cool stuff. I&rsquo;m just going to show you how to make a silly, over-simplified, bot-type-thingy that sorta-kinda responds to user input. Ish.</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
</pre></td><td class='code'><pre><code class='ruby'><span class='line'><span class="k">class</span> <span class="nc">ChatterBox</span>
</span><span class='line'>  <span class="no">COMMANDS</span> <span class="o">=</span> <span class="o">[</span><span class="ss">:hi</span><span class="p">,</span> <span class="ss">:how</span><span class="p">,</span> <span class="ss">:why</span><span class="p">,</span> <span class="ss">:when</span><span class="p">,</span> <span class="ss">:who</span><span class="p">,</span> <span class="ss">:where</span><span class="p">,</span> <span class="ss">:what</span><span class="p">,</span> <span class="ss">:bye</span><span class="o">]</span>
</span><span class='line'>
</span><span class='line'><span class="k">end</span>
</span></code></pre></td></tr></table></div></figure>


<p>Boom…done! Ok, kidding. But we&rsquo;ve gotten started at least. Here we&rsquo;re just defining a <code>class</code> called <code>ChatterBox</code> that we&rsquo;ll be using to demonstrate a neat little use of <code>.send</code> that comes in handy. Maybe not brain-explode-all-over-your-face handy, but huh-would-you-look-at-that handy. The <code>COMMANDS = [blah, blah, blah]</code> bit is going to be useful in just a second. Actually, it&rsquo;s going to be useful right now.</p>

<p>Let&rsquo;s pretend for a second that we have some fancy pants command line interface that we can wrap around this sucker and that everytime a user types something in, that sentence gets passed to a <code>.call</code> method on our <code>ChatterBox</code>. Cool? Cool.</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
</pre></td><td class='code'><pre><code class='ruby'><span class='line'><span class="k">class</span> <span class="nc">ChatterBox</span>
</span><span class='line'>  <span class="no">COMMANDS</span> <span class="o">=</span> <span class="o">[</span><span class="ss">:hi</span><span class="p">,</span> <span class="ss">:how</span><span class="p">,</span> <span class="ss">:why</span><span class="p">,</span> <span class="ss">:when</span><span class="p">,</span> <span class="ss">:who</span><span class="p">,</span> <span class="ss">:where</span><span class="p">,</span> <span class="ss">:what</span><span class="p">,</span> <span class="ss">:bye</span><span class="o">]</span>
</span><span class='line'>
</span><span class='line'>  <span class="k">def</span> <span class="nf">call</span>
</span><span class='line'>
</span><span class='line'>  <span class="k">end</span>
</span><span class='line'><span class="k">end</span>
</span></code></pre></td></tr></table></div></figure>


<p>Sweet. Now let&rsquo;s make it do something. I&rsquo;m going to throw a bunch of code in there, and then we&rsquo;ll chat (hah again!) about it:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
<span class='line-number'>11</span>
<span class='line-number'>12</span>
<span class='line-number'>13</span>
<span class='line-number'>14</span>
<span class='line-number'>15</span>
<span class='line-number'>16</span>
</pre></td><td class='code'><pre><code class='ruby'><span class='line'><span class="k">class</span> <span class="nc">ChatterBox</span>
</span><span class='line'>
</span><span class='line'>  <span class="no">COMMANDS</span> <span class="o">=</span> <span class="o">[</span><span class="ss">:hi</span><span class="p">,</span> <span class="ss">:how</span><span class="p">,</span> <span class="ss">:why</span><span class="p">,</span> <span class="ss">:when</span><span class="p">,</span> <span class="ss">:who</span><span class="p">,</span> <span class="ss">:where</span><span class="p">,</span> <span class="ss">:what</span><span class="p">,</span> <span class="ss">:bye</span><span class="o">]</span>
</span><span class='line'>
</span><span class='line'>  <span class="k">def</span> <span class="nf">call</span><span class="p">(</span><span class="n">sentence</span><span class="p">)</span>
</span><span class='line'>    <span class="n">sentence</span><span class="o">.</span><span class="n">split</span><span class="o">.</span><span class="n">each_with_index</span> <span class="k">do</span> <span class="o">|</span><span class="n">word</span><span class="p">,</span> <span class="n">i</span><span class="o">|</span>
</span><span class='line'>      <span class="k">if</span> <span class="nb">self</span><span class="o">.</span><span class="n">class</span><span class="o">::</span><span class="no">COMMANDS</span><span class="o">.</span><span class="n">include</span><span class="p">(</span><span class="n">word</span><span class="o">.</span><span class="n">downcase</span><span class="o">.</span><span class="n">gsub</span><span class="p">(</span><span class="sr">/[.?!,&#39;]/</span><span class="p">,</span><span class="s1">&#39;&#39;</span><span class="p">)</span><span class="o">.</span><span class="n">to_sym</span><span class="p">)</span>
</span><span class='line'>        <span class="nb">self</span><span class="o">.</span><span class="n">send</span><span class="p">(</span><span class="s2">&quot;</span><span class="si">#{</span><span class="n">word</span><span class="o">.</span><span class="n">downcase</span><span class="o">.</span><span class="n">strip</span><span class="o">.</span><span class="n">gsub</span><span class="p">(</span><span class="sr">/[.?!,&#39;]/</span><span class="p">,</span><span class="s1">&#39;&#39;</span><span class="p">)</span><span class="si">}</span><span class="s2">&quot;</span><span class="p">,</span> <span class="n">sentence</span><span class="o">.</span><span class="n">downcase</span><span class="o">.</span><span class="n">gsub</span><span class="p">(</span><span class="sr">/[.?!,&#39;]/</span><span class="p">,</span><span class="s1">&#39;&#39;</span><span class="p">)</span><span class="o">.</span><span class="n">split</span><span class="p">)</span>
</span><span class='line'>        <span class="k">break</span>
</span><span class='line'>      <span class="k">elsif</span> <span class="n">i</span> <span class="o">==</span> <span class="n">sentence</span><span class="o">.</span><span class="n">length</span> <span class="o">-</span> <span class="mi">1</span>
</span><span class='line'>        <span class="nb">puts</span> <span class="s2">&quot;I have no idea what you&#39;re talking about.&quot;</span>
</span><span class='line'>      <span class="k">end</span>
</span><span class='line'>    <span class="k">end</span>
</span><span class='line'>  <span class="k">end</span>
</span><span class='line'>
</span><span class='line'><span class="k">end</span>
</span></code></pre></td></tr></table></div></figure>


<p>The first line is pretty basic: it takes the passed in argument, sentence, splits it up (by default that happens at the spaces) using the <code>.split</code> method, and iterates through that newly-minted <code>array</code>, while keeping track of the index.</p>

<p>And now we move on to the dopeness that is <code>.send</code>.</p>

<p><code>COMMANDS</code> is just our array of possible commands we want to accept from the user. We first check to see if any word in the sentence that was passed in, when converted to a <code>sybmol</code> matches one of our possible commands. If it does, we get to break out the magic. <code>.send</code>, remember, works like this:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class='ruby'><span class='line'><span class="n">object</span><span class="o">.</span><span class="n">send</span><span class="p">(</span><span class="s2">&quot;method_as_string&quot;</span><span class="p">,</span> <span class="n">arguments</span><span class="p">)</span>
</span></code></pre></td></tr></table></div></figure>


<p>So in this case, our <code>object</code> is our instance of the <code>ChatterBox</code> class, and we send it, as a method, the word that matched one of our possible commands. Then, we send the rest of the sentence along as arguments. <code>.send</code> takes care of all of this for us. It converts the <code>string</code> version of the command into a symbol and uses that to call the method of the same name that we&rsquo;ve written into the class. To that method, it passes the other non-matching part of the sentence.</p>

<p>In other words, say we do something like this:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
</pre></td><td class='code'><pre><code class='ruby'><span class='line'><span class="n">c</span> <span class="o">=</span> <span class="no">ChatterBox</span><span class="o">.</span><span class="n">new</span>
</span><span class='line'><span class="n">c</span><span class="o">.</span><span class="n">call</span><span class="p">(</span><span class="s2">&quot;What are you doing tonight?&quot;</span><span class="p">)</span>
</span></code></pre></td></tr></table></div></figure>


<p>In the sentence we passed to the <code>.call</code> method, there is one of our commands: &lsquo;What&rsquo;. <code>.send</code> then gets a hold of this in our <code>.call</code> method and turns it into this:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class='ruby'><span class='line'><span class="n">c</span><span class="o">.</span><span class="n">send</span><span class="p">(</span><span class="s2">&quot;what&quot;</span><span class="p">,</span> <span class="o">[</span><span class="s2">&quot;are&quot;</span><span class="p">,</span> <span class="s2">&quot;you&quot;</span><span class="p">,</span> <span class="s2">&quot;doing&quot;</span><span class="p">,</span> <span class="s2">&quot;tonight&quot;</span><span class="o">]</span><span class="p">)</span>
</span></code></pre></td></tr></table></div></figure>


<p>And remembering how the <code>.send</code> method works, we know that this is going to call a method, <code>.what</code> and send it <code>["are", "you", "doing", "tonight"]</code> as an argument.</p>

<p>So it&rsquo;s essentially doing this:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
</pre></td><td class='code'><pre><code class='ruby'><span class='line'><span class="n">c</span><span class="o">.</span><span class="n">what</span><span class="p">(</span><span class="o">[</span><span class="s2">&quot;are&quot;</span><span class="p">,</span> <span class="s2">&quot;you&quot;</span><span class="p">,</span> <span class="s2">&quot;doing&quot;</span><span class="p">,</span> <span class="s2">&quot;tonight&quot;</span><span class="o">]</span><span class="p">)</span>
</span></code></pre></td></tr></table></div></figure>


<p>But we didn&rsquo;t actually have to hard-code in a call to that <code>.what</code> method.</p>

<p>Pretty neat, right? Based on a passed in sentence, we can fire off a specific method and then use that method to parse a sentence in any way that we want.</p>

<p>One of these (reallyverymessymethodsthatyoushouldonlylookatforliketwosecondssoyoureyesdon&#8217;tmelt) methods might look something like this:</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
<span class='line-number'>11</span>
<span class='line-number'>12</span>
<span class='line-number'>13</span>
<span class='line-number'>14</span>
<span class='line-number'>15</span>
<span class='line-number'>16</span>
<span class='line-number'>17</span>
<span class='line-number'>18</span>
<span class='line-number'>19</span>
<span class='line-number'>20</span>
<span class='line-number'>21</span>
</pre></td><td class='code'><pre><code class='ruby'><span class='line'><span class="k">class</span> <span class="nc">ChatterBox</span>
</span><span class='line'>
</span><span class='line'>  <span class="no">COMMANDS</span> <span class="o">=</span> <span class="o">[</span><span class="ss">:hi</span><span class="p">,</span> <span class="ss">:how</span><span class="p">,</span> <span class="ss">:why</span><span class="p">,</span> <span class="ss">:when</span><span class="p">,</span> <span class="ss">:who</span><span class="p">,</span> <span class="ss">:where</span><span class="p">,</span> <span class="ss">:what</span><span class="p">,</span> <span class="ss">:bye</span><span class="o">]</span>
</span><span class='line'>
</span><span class='line'>  <span class="k">def</span> <span class="nf">what</span><span class="p">(</span><span class="n">args</span><span class="p">)</span>
</span><span class='line'>    <span class="k">if</span> <span class="n">args</span><span class="o">.</span><span class="n">include?</span><span class="p">(</span><span class="s2">&quot;is&quot;</span><span class="p">)</span> <span class="o">&amp;&amp;</span> <span class="n">args</span><span class="o">.</span><span class="n">include?</span><span class="p">(</span><span class="s2">&quot;your&quot;</span><span class="p">)</span>
</span><span class='line'>      <span class="nb">puts</span> <span class="s2">&quot;Personal! Sheesh!&quot;</span>
</span><span class='line'>    <span class="k">elsif</span> <span class="n">args</span><span class="o">.</span><span class="n">include?</span><span class="p">(</span><span class="s2">&quot;is&quot;</span><span class="p">)</span> <span class="o">||</span> <span class="n">args</span><span class="o">.</span><span class="n">include?</span><span class="p">(</span><span class="s2">&quot;are&quot;</span><span class="p">)</span>
</span><span class='line'>      <span class="nb">puts</span> <span class="s2">&quot;Get a dictionary, bonehead. I&#39;m not Wikipedia over here.&quot;</span>
</span><span class='line'>    <span class="k">elsif</span> <span class="n">args</span><span class="o">.</span><span class="n">include?</span><span class="p">(</span><span class="s2">&quot;should&quot;</span><span class="p">)</span>
</span><span class='line'>      <span class="nb">puts</span> <span class="s2">&quot;I ain&#39;t a fortune teller, kid. And I certainly don&#39;t work for free.&quot;</span>
</span><span class='line'>    <span class="k">elsif</span> <span class="n">args</span><span class="o">.</span><span class="n">include?</span><span class="p">(</span><span class="s2">&quot;are&quot;</span><span class="p">)</span>
</span><span class='line'>      <span class="nb">puts</span> <span class="s2">&quot;That&#39;s none of your business. I do what I do. Ya herd?&quot;</span>
</span><span class='line'>    <span class="k">else</span>
</span><span class='line'>      <span class="nb">puts</span> <span class="s2">&quot;Huh?&quot;</span>
</span><span class='line'>    <span class="k">end</span>
</span><span class='line'>  <span class="k">end</span>
</span><span class='line'>
</span><span class='line'>  <span class="o">.</span><span class="n">.</span><span class="o">.</span>
</span><span class='line'>
</span><span class='line'><span class="k">end</span>
</span></code></pre></td></tr></table></div></figure>


<p>I&rsquo;ll let you go through that and see how poorly a person can actually write a bunch of <code>if</code> statements, but it&rsquo;s pretty fun to run if you add similar methods for the other possible commands, if I do say so myself. Here&rsquo;s some sample output from this (ignore the syntax highlighting again):</p>

<figure class='code'><figcaption><span></span></figcaption><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
<span class='line-number'>11</span>
<span class='line-number'>12</span>
<span class='line-number'>13</span>
<span class='line-number'>14</span>
<span class='line-number'>15</span>
<span class='line-number'>16</span>
<span class='line-number'>17</span>
<span class='line-number'>18</span>
<span class='line-number'>19</span>
<span class='line-number'>20</span>
<span class='line-number'>21</span>
<span class='line-number'>22</span>
</pre></td><td class='code'><pre><code class='ruby'><span class='line'><span class="n">t</span> <span class="o">=</span> <span class="no">ChatterBox</span><span class="o">.</span><span class="n">new</span>
</span><span class='line'>
</span><span class='line'><span class="n">t</span><span class="o">.</span><span class="n">call</span><span class="p">(</span><span class="s2">&quot;What is your favorite color?&quot;</span><span class="p">)</span>
</span><span class='line'><span class="o">=&gt;</span> <span class="no">Personal</span><span class="o">!</span> <span class="no">Sheesh</span><span class="o">!</span>
</span><span class='line'><span class="n">t</span><span class="o">.</span><span class="n">call</span><span class="p">(</span><span class="s2">&quot;Hi there!&quot;</span><span class="p">)</span>
</span><span class='line'><span class="o">=&gt;</span> <span class="no">Well</span> <span class="n">hi</span> <span class="n">there!</span> <span class="no">Nice</span> <span class="n">to</span> <span class="n">see</span> <span class="n">you</span><span class="o">.</span> <span class="p">:)</span>
</span><span class='line'><span class="n">t</span><span class="o">.</span><span class="n">call</span><span class="p">(</span><span class="s2">&quot;Why can&#39;t we be friends?&quot;</span><span class="p">)</span>
</span><span class='line'><span class="o">=&gt;</span> <span class="no">We</span><span class="s1">&#39;d never work together. It&#39;</span><span class="n">s</span> <span class="n">so</span> <span class="n">obvious</span><span class="o">.</span>
</span><span class='line'><span class="n">t</span><span class="o">.</span><span class="n">call</span><span class="p">(</span><span class="s2">&quot;How old are you?&quot;</span><span class="p">)</span>
</span><span class='line'><span class="o">=&gt;</span> <span class="no">Younger</span> <span class="n">than</span> <span class="n">yo</span> <span class="n">mama!</span>
</span><span class='line'><span class="n">t</span><span class="o">.</span><span class="n">call</span><span class="p">(</span><span class="s2">&quot;What is the meaning of life?&quot;</span><span class="p">)</span>
</span><span class='line'><span class="o">=&gt;</span> <span class="no">Get</span> <span class="n">a</span> <span class="n">dictionary</span><span class="p">,</span> <span class="n">bonehead</span><span class="o">.</span> <span class="n">I</span><span class="s1">&#39;m not Wikipedia over here.</span>
</span><span class='line'><span class="s1">t.call(&quot;Where were you born?&quot;)</span>
</span><span class='line'><span class="s1">=&gt; Probably on the moon.</span>
</span><span class='line'><span class="s1">t.call(&quot;Who am I?&quot;)</span>
</span><span class='line'><span class="s1">=&gt; How in the world could I know that? You didn&#39;</span><span class="n">t</span> <span class="n">tell</span> <span class="n">me</span> <span class="n">your</span> <span class="nb">name</span><span class="o">!</span>
</span><span class='line'><span class="n">t</span><span class="o">.</span><span class="n">call</span><span class="p">(</span><span class="s2">&quot;When can I meet your family?&quot;</span><span class="p">)</span>
</span><span class='line'><span class="o">=&gt;</span> <span class="n">A</span> <span class="n">long</span> <span class="n">time</span> <span class="n">from</span> <span class="n">now</span><span class="o">.</span>
</span><span class='line'><span class="n">t</span><span class="o">.</span><span class="n">call</span><span class="p">(</span><span class="s2">&quot;How is cheese made?&quot;</span><span class="p">)</span>
</span><span class='line'><span class="o">=&gt;</span> <span class="n">I</span><span class="s1">&#39;m not too sure how...I&#39;</span><span class="n">m</span> <span class="n">a</span> <span class="n">computer</span><span class="p">,</span> <span class="n">after</span> <span class="n">all</span><span class="o">.</span>
</span><span class='line'><span class="n">t</span><span class="o">.</span><span class="n">call</span><span class="p">(</span><span class="s2">&quot;Gobledygook blah pants&quot;</span><span class="p">)</span>
</span><span class='line'><span class="o">=&gt;</span> <span class="n">I</span> <span class="n">have</span> <span class="n">no</span> <span class="n">idea</span> <span class="n">what</span> <span class="n">you</span><span class="err">&#39;</span><span class="n">re</span> <span class="n">talking</span> <span class="n">about</span><span class="o">.</span>
</span></code></pre></td></tr></table></div></figure>


<p>Hey, look! We made a really pathetic AIM bot, minus AIM and the internet and other stuff. Ok, really we just made a totally useless program to create a contrived example with which to demonstrate <code>.send</code>. Still, though, it&rsquo;s really awesome. <code>.send</code> is really awesome, to clarify, not this program. And supremely useful if you want to dynamically call methods based on user input (or in other situations where you don&rsquo;t really know what method is going to be called).</p>

<p>Not too shabby, Ruby. Not too shabby.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[Jason Arhart Talks RSpec]]></title>
    <link href="http://loganhasson.github.io/blog/2013/10/07/jason-arhart-talks-rspec/"/>
    <updated>2013-10-07T23:31:00-04:00</updated>
    <id>http://loganhasson.github.io/blog/2013/10/07/jason-arhart-talks-rspec</id>
    <content type="html"><![CDATA[<p>We&rsquo;ve been spending a lot of time thinking about and implementing test-driven development
using RSpec at <a href="http://www.flatironschool.com">Flatiron School</a> these past several days.
While I certainly love the <em>idea</em> of loving TDD, I&rsquo;ve found myself struggling to understand exactly
how to effectively start dipping my toe into the Ruby testing pool.</p>

<p>It&rsquo;s not that I don&rsquo;t grasp the concept of testing first. Rather, I haven&rsquo;t been able to
figure out the appropriate tests to write before I actually start coding. I often find myself combining
the following steps:</p>

<pre><code>1. Pseudo coding a solution to the programming problem
2. Thinking of how I could test that pseudo code
3. Thinking of how I could turn that pseudo code into Ruby
4. Thinking of tests to write to test that code
</code></pre>

<p>Sadly, this creates a very un-TDD workflow. I either write tests that are completely
unrelated to my eventual code, or I end up writing code and then creating tests that
conform to that code.</p>

<p>I came across a wonderful <a href="https://speakerdeck.com/lvrug/rspec-for-beginners-jason-arhart">deck by Jason Arhart</a> today, though,
that has finally (sort of) given me a sense of how to approach this whole TDD thing. This slide in particular was my &ldquo;aha&rdquo;
moment&hellip;</p>

<script async class="speakerdeck-embed" data-slide="11" data-id="ec123320ddad0130679a56e32e49313d" data-ratio="1.74446337308348" src="http://loganhasson.github.io//speakerdeck.com/assets/embed.js"></script>


<p>It seems so simple, but the line &ldquo;focus on <strong>what</strong> before <strong>how</strong>&rdquo; is quite a brilliant way of putting it.</p>

<p>I&rsquo;d been struggling to write effective RSpec tests (let alone even come up with tests <em>at all</em>) because
I was approaching the whole process with a &ldquo;how&rdquo; mindset. I thought of my tests in the context of how I&rsquo;d be solving the
programming problem that was before me, rather than as a way to clearly document and test the &ldquo;what&rdquo; of the problem. The distinction is nuanced, but the way to properly test is to think, &ldquo;what should my methods/objects/whatevers be doing?&rdquo; instead of &ldquo;how am I going to get my methods/objects/whatevers to solve this problem and also prove to some test that they are working?&rdquo;</p>

<p>Of course, I still couldn&rsquo;t whip together a good test suite right now, but this presentation has given me much more clarity as to how I should actually approach writing passable (hah!) test stuites in the near future.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[bash trash]]></title>
    <link href="http://loganhasson.github.io/blog/2013/09/25/bash-trash/"/>
    <updated>2013-09-25T16:48:00-04:00</updated>
    <id>http://loganhasson.github.io/blog/2013/09/25/bash-trash</id>
    <content type="html"><![CDATA[<p>This isn&rsquo;t too terribly exciting, but I&rsquo;ve been on a big &ldquo;write-everything-in-fact-<em>all-the-things</em>-as-functions-for-bash&rdquo; kick these last couple of days, and I am pretty happy with my latest one. Though I rarely find myself in the position of having files in my Trash any more, I still occasionally drag something in there from my downloads folder (since they are right next door to one another on my dock). I hate, hate, hate, hate, hate having a cluttered Trash icon, so I empty it all the time.</p>

<p>While it&rsquo;s totally possible to just press <code>Shift+Command+Delete</code> while in Finder, I&rsquo;d much rather not have to switch out of terimal if possible. So, I wrote this little function and threw it in my .bash_profile. Now, whenever I want to empty the trash, I just have to type <code>empty</code>. It&rsquo;s lovely. And as a bonus, I even get to see how many files were deleted.</p>

<figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>function empty () {
</span><span class='line'>  pushd ~/.Trash &gt; /dev/null
</span><span class='line'>  tmp=$(rm -rfv * | wc -l | sed -e 's/^[ \t]*//')
</span><span class='line'>  if [ $tmp == "1" ]; then
</span><span class='line'>    echo "$tmp file was removed."
</span><span class='line'>  else
</span><span class='line'>    echo "$tmp files were removed."
</span><span class='line'>  fi
</span><span class='line'>  pushd &gt; /dev/null
</span><span class='line'>}</span></code></pre></td></tr></table></div></figure>


<p>Here&rsquo;s a commented version if you&rsquo;re interested. (Note, I looked up the regular expression for one of the commands. Regular expressions are hard.)</p>

<figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
<span class='line-number'>3</span>
<span class='line-number'>4</span>
<span class='line-number'>5</span>
<span class='line-number'>6</span>
<span class='line-number'>7</span>
<span class='line-number'>8</span>
<span class='line-number'>9</span>
<span class='line-number'>10</span>
<span class='line-number'>11</span>
<span class='line-number'>12</span>
<span class='line-number'>13</span>
<span class='line-number'>14</span>
<span class='line-number'>15</span>
<span class='line-number'>16</span>
<span class='line-number'>17</span>
<span class='line-number'>18</span>
<span class='line-number'>19</span>
<span class='line-number'>20</span>
<span class='line-number'>21</span>
<span class='line-number'>22</span>
<span class='line-number'>23</span>
<span class='line-number'>24</span>
<span class='line-number'>25</span>
<span class='line-number'>26</span>
<span class='line-number'>27</span>
<span class='line-number'>28</span>
<span class='line-number'>29</span>
<span class='line-number'>30</span>
<span class='line-number'>31</span>
<span class='line-number'>32</span>
<span class='line-number'>33</span>
</pre></td><td class='code'><pre><code class=''><span class='line'># Commented Version
</span><span class='line'># USE: empty
</span><span class='line'> 
</span><span class='line'>function empty () {
</span><span class='line'>  # push the CWD into the stack and change to the ~/.Trash directory. Pipe this output to /dev/null so it doesn't print to
</span><span class='line'>  # the console
</span><span class='line'>
</span><span class='line'>  pushd ~/.Trash &gt; /dev/null
</span><span class='line'>
</span><span class='line'>  # assign a variable, tmp, to the following
</span><span class='line'>  # rm -rfv * deletes everything in the trash. the -v flag tells it to echo each file as it deletes
</span><span class='line'>  # this is then piped to wc -l, which counts the lines that were outputted
</span><span class='line'>  # that count gets a bunch of whitespace put in front of it for some reason, and is piped to the stream editor (sed), and the
</span><span class='line'>  # -e flag lets us pass a fancy regex to strip the leading whitespace
</span><span class='line'>
</span><span class='line'>  tmp=$(rm -rfv * | wc -l | sed -e 's/^[ \t]*//')
</span><span class='line'>
</span><span class='line'>  # check to see if $tmp is 1, if so we want to echo singular "file"
</span><span class='line'>
</span><span class='line'>  if [ $tmp == "1" ]; then
</span><span class='line'>    echo "$tmp file was removed."
</span><span class='line'>  else
</span><span class='line'>
</span><span class='line'>  # otherwise echo plural "files"
</span><span class='line'>
</span><span class='line'>    echo "$tmp files were removed."
</span><span class='line'>  fi
</span><span class='line'>
</span><span class='line'>  # pushd again brings us back to the directory we were in before running the command, and the output is again piped to
</span><span class='line'>  # /dev/null
</span><span class='line'>
</span><span class='line'>  pushd &gt; /dev/null
</span><span class='line'>}</span></code></pre></td></tr></table></div></figure>

]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[understanding rebasing]]></title>
    <link href="http://loganhasson.github.io/blog/2013/09/24/understanding-rebasing/"/>
    <updated>2013-09-24T16:48:00-04:00</updated>
    <id>http://loganhasson.github.io/blog/2013/09/24/understanding-rebasing</id>
    <content type="html"><![CDATA[<p>Today was full of pulling and fetching and branching and merging and commiting and, yes, the dreaded <strong>rebasing</strong>. I&rsquo;ll admit: before today, though I&rsquo;d read about it on multiple occasions, rebasing in Git still made my brain physically hurt. And even as today wore on, I found myself more and more perplexed by this scary, repo-busting command.</p>

<p>Until I had an epiphany.</p>

<p>Git rebasing makes sense when you realize that <em>it does exactly what the name says it&rsquo;ll do.</em> Stick with me here.</p>

<p>Let&rsquo;s, for a second, imagine we have a repo that looks something like this:</p>

<p><img src="http://loganhasson.github.io/images/repo-1.jpg" alt="A possible Git repository" /></p>

<p>Now, let&rsquo;s throw out all we know about rebasing (or think we know about rebasing), and just concentrate on what the command actually says. If we rebase <code>feature</code> onto <code>master</code>, we are literally changing the base (or parent!) of <code>feature</code>.</p>

<p>Let&rsquo;s look at the repo again, but this time with a bit more descriptors:</p>

<p><img src="http://loganhasson.github.io/images/repo-2.jpg" alt="A more verbose Git repository" /></p>

<p>As it stands, <code>feature</code> begins it&rsquo;s life as a child of our second commit. If we <code>rebase</code> it onto <code>master</code> we are essentially saying, &ldquo;Nope, let&rsquo;s rewrite history. Let&rsquo;s pretend that <code>feature</code> began it&rsquo;s life as a child of our sixth commit (the most current commit in the <code>master</code> branch).&rdquo; So to make this rebase happen, we run the following commands:</p>

<figure class='code'><div class="highlight"><table><tr><td class="gutter"><pre class="line-numbers"><span class='line-number'>1</span>
<span class='line-number'>2</span>
</pre></td><td class='code'><pre><code class=''><span class='line'>git checkout feature
</span><span class='line'>git rebase master</span></code></pre></td></tr></table></div></figure>


<p>What Git does, to rewrite history, is take every commit between <strong>C2</strong> and <strong>C6</strong> (inclusive) and apply it to the <code>feature</code> branch. Because, for the <code>feature</code> branch to have a new base, it needs to know the whole history of that particlar base (parent).</p>

<p>(Essentially, <code>feature</code> needs to get caught up. And for that to happen, it needs to &ldquo;know&rdquo; everything the <code>master</code> branch &ldquo;knows&rdquo; up until the current moment. And how can that happen? Git has to, one by one, apply every change (commit) in <code>master</code>&rsquo;s history to <code>feature</code>.)</p>

<p>Now, our repo looks like this:</p>

<p><img src="http://loganhasson.github.io/images/repo-3.jpg" alt="A Git repository after rebasing" /></p>

<p>Woah! Now we have a nice clean history (it&rsquo;s all linear and stuff!), but <code>feature</code> is ahead of <code>master</code>. Why is that?</p>

<p>This is what initially confused the heck out of me, once I thought I understood rebasing. There&rsquo;s two things to keep in mind for this to make sense:</p>

<ul>
<li>The whole &ldquo;rebase onto&rdquo; nomenclature is, in and of itself, somewhat confusing.</li>
<li>If we hadn&rsquo;t rebased or merged, the <code>master</code> branch would never have any concept of the <code>feature</code> branch after our second commit.</li>
</ul>


<p>Let&rsquo;s tackle these points one at a time.</p>

<p>1) <em>The whole &ldquo;rebase onto&rdquo; nomenclature is, in and of itself, somewhat confusing.</em></p>

<p>Here&rsquo;s the thing. Everything I&rsquo;ve written before this is based upon the fact that the <code>rebase</code> command actually does what it&rsquo;s name implies. Cool. However, when we add the word &ldquo;onto&rdquo; into the mix, things get confusing. To me, <code>rebase onto</code> (combined with the myriad descriptions of rebasing as the rewinding of and playing back of commits onto one branch or another) makes it seem as if the branch being, erm, rebased onto, is being acted upon in some way.</p>

<p><strong>It isn&rsquo;t.</strong></p>

<p>2) <em>If we hadn&rsquo;t rebased or merged, the <code>master</code> branch would never have any concept of the <code>feature</code> branch after our second commit.</em></p>

<p>Let&rsquo;s look at another version of our original repo, but with some files added to the mix. (Each time you see a file name next to a commit, this means the file was created and commited in that commit. These are simplistic commits for explanation purposes.)</p>

<p><img src="http://loganhasson.github.io/images/repo-4.jpg" alt="A Git repository with some files" /></p>

<p>Cool. Now let&rsquo;s look at the same repository, but this time with the files carried through from one commit to the next. In other words, now we&rsquo;ll look at what files each commit knows about:</p>

<p><img src="http://loganhasson.github.io/images/repo-5.jpg" alt="A Git repository with some files and inheritance" /></p>

<p>Now, if we created a new branch off of <code>master</code> right now, what files would it inherit?</p>

<p>If you said <em>files 1, 2, 3, 8, 9, and 10</em>, then you&rsquo;d be right.</p>

<p>So if we rebase a branch onto <code>master</code> now, it too would know about those files. (It&rsquo;s new parent is our current <code>master</code> branch, and thus inherits everything from that branch.)</p>

<p><img src="http://loganhasson.github.io/images/repo-6.jpg" alt="A Git repository with some files and inheritance after rebase" /></p>

<p>But if you notice from that image above, our <code>master</code> branch has no clue that <em>files 4, 5, 6, and 7</em> exist. Rebasing our <code>feature</code> branch, which does know about those files, onto <code>master</code> just means that now <code>feature</code> knows about <em>files 1, 2, 3, 8, 9, and 10</em>, just as any new branch would at this point!</p>

<p>Aha! So in order to catch <code>master</code> up on all the lovely things that we&rsquo;ve been working on in <code>feature</code> branch, we still need to merge after the rebase is complete. And then, we get this:</p>

<p><img src="http://loganhasson.github.io/images/repo-7.jpg" alt="A Git repository after rebase and merge" /></p>

<p>But, bonus side effect: we don&rsquo;t have to worry about conflicts. Sweet.</p>

<p>Phew. Git rocks.</p>
]]></content>
  </entry>
  
  <entry>
    <title type="html"><![CDATA[hello world]]></title>
    <link href="http://loganhasson.github.io/blog/2013/09/24/hello-world/"/>
    <updated>2013-09-24T16:48:00-04:00</updated>
    <id>http://loganhasson.github.io/blog/2013/09/24/hello-world</id>
    <content type="html"><![CDATA[<p>Hi, kids. The name&rsquo;s Logan. Let&rsquo;s see what happens when I go ahead and publish this post.</p>

<p>&hellip;</p>

<p>Look, it worked!</p>

<p>Go checkout my first <a href="http://loganhasson.github.io/blog/2013/09/24/understanding-rebasing/">real post</a></p>
]]></content>
  </entry>
  
</feed>
